var ImageInterface = require('../src/Image');
var Box = require('../src/Box');

module.exports = class Image extends ImageInterface {
	constructor(image) {
		super();
		this._image = image;
	}

	getSize() {
		return Promise.resolve(new Box(this._image.width(), this._image.height()));
	}
}