var NoCapacityError = require('./NoCapacityError');
var Image;

module.exports = Image = class {
	/**
		@returns {Promise.<Box>}
	*/
	getSize() {
		throw new NoCapacityError();
	}

	/**
		@returns {Promise.<Image>}
	*/
	resize(box, mode) {
		throw new NoCapacityError();
	}

	/**
		@returns {Promise.<Image>}
	*/
	crop(zone) {
		throw new NoCapacityError();
	}

	/**
		@returns {Promise.<Image>}
	*/
	rotate(degree) {
		throw new NoCapacityError();
	}

	/**
	*	@returns {Promise.<Image>}
	*	quality 0-100
	*/
	saveJPEG(file, quality) {
		throw new NoCapacityError();
	}

	/**
		@returns {Promise.<Buffer>}
	*/
	toBufferJPEG(quality) {
		throw new NoCapacityError();
	}
}

Image.INTERPOLATION_NONE = null;
Image.INTERPOLATION_LINEAR = 'linear';
Image.INTERPOLATION_CUBIC = 'cubic';
Image.INTERPOLATION_LANCZOS = 'lanczos';